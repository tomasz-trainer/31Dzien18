<Window x:Class="P03WeatherForecastWPF.Client.ProductDetailsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:P03WeatherForecastWPF.Client"
        mc:Ignorable="d"
        Closing="Window_Closing"
        Title="ProductDetailsView" Height="Auto" Width="400" Padding="20">
    <StackPanel>
        <Grid DataContext="{Binding SelectedProduct}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="23*"></ColumnDefinition>
                <ColumnDefinition Width="88*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Title:" Grid.Row="0" Grid.Column="0" Margin="0,0,0,18" Grid.RowSpan="2"></TextBlock>
            <TextBox Text="{Binding Title}" Grid.Row="0" Grid.ColumnSpan="3" Margin="67,0,0,18" Grid.RowSpan="2"/>

            <TextBlock Text="Description:" Grid.Row="1" Grid.Column="0" Margin="0,0,0,18" Grid.RowSpan="2"></TextBlock>
            <TextBox Text="{Binding Description}" Grid.Row="1" Grid.ColumnSpan="3" Margin="67,0,0,18" Grid.RowSpan="2"/>

            <TextBlock Text="Barcode:" Grid.Row="2" Grid.Column="0" Margin="0,0,0,18" Grid.RowSpan="2"></TextBlock>
            <TextBox Text="{Binding Barcode}" Grid.Row="2" Grid.ColumnSpan="3" Margin="67,0,0,18" Grid.RowSpan="2"/>

            <TextBlock Text="Price:" Grid.Row="3" Grid.Column="0" Margin="0,0,0,18" Grid.RowSpan="2"></TextBlock>
            <TextBox Text="{Binding Price}" Grid.Row="3" Grid.ColumnSpan="3" Margin="67,0,0,18" Grid.RowSpan="2"/>

            <TextBlock Text="ReleaseDate:" Grid.Row="4" Grid.Column="0"></TextBlock>
            <TextBox Text="{Binding ReleaseDate}" Grid.Row="4" Grid.ColumnSpan="3" Margin="67,0,0,0"/>


        </Grid>
        <StackPanel>
            <Button Content="Save" Command="{Binding SaveProductCommand}"></Button>
            <Button Content="Delete" Command="{Binding DeleteProductCommand}"></Button>
        </StackPanel>
    </StackPanel>
</Window>
